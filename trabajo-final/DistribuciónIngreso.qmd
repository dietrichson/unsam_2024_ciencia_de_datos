---
title: "Distribución del ingreso"
author: "Sabrina Scarnato"
bibliography: references.bib
---

## Introducción

Este capítulo se propone analizar, a través de los principales indicadores, la desigualdad económica en Argentina entre los años 2022 y 2023 en las regiones de Gran Buenos Aires, Noroeste, Noreste, Pampeana, Cuyo y Patagonia. Esto se realizará con un análisis cualitativo sobre los datos informados por el Instituto Nacional de Estadística y Censos [@indec].

El análisis se centrará en el Ingreso Total Familiar (ITF) como un indicador clave para distinguir las áreas del país donde existen mayores diferencias entre las familias con ingresos altos y aquellas cuyos ingresos se sitúan por debajo de la Canasta Básica Total. Este enfoque permitirá identificar las regiones y sectores más afectados por la desigualdad, proporcionando una visión detallada de cómo se distribuye la riqueza a lo largo del territorio nacional.

## Desarrollo metodológico

### Tipo de investigación

Para esta investigación, decidí utilizar un enfoque metodológico cuantitativo con el objetivo de obtener un resultado riguroso del fenómeno de la distribución del ingreso familiar en el país.

El método cuantitativo es, según David Alan Neill y Liliana Cortez Suárez (2018), una forma estructurada de recopilar y analizar datos de diversas fuentes que utiliza computadoras, herramientas estadísticas y matemáticas para producir resultados. Recopila datos numéricos y estadísticos con el objetivo claro de obtener “objetividad” a través de la cuantificación.

### Datos

Los datos fueron recopilados de la encuesta permanente de hogares realizada por el Instituto Nacional de Estadística y Censos (INDEC). Pertenecen al 4to trimestre del 2022 y 2023 de todo el país.

### Objetivo

Indagar cómo es la distribución del ingreso en las diversas regiones de Argentina durante el lapso de tiempo seleccionado.

### Pregunta de investigación

¿Hay diferencias en la distribución de ingresos entre las regiones? De ser así, ¿Cuáles son las que tienen mayor disparidad?

¿Cuanto es la diferencia entre la media de las regiones?

### Procedimiento

Para llevar a cabo el análisis procederé a:

1.  Cargar los datos del informe.

2.  A través de la librería tidyverse y utilizando el indicador de Ingreso Total Familiar (ITF), filtrar por regiones.

3.  Generar gráficos para poder visualizar los datos obtenidos.

### Herramientas de análisis

Usamos el lenguaje R [@base] con los paquetes de tidyverse [@tidyverse]. Usamos el sistema Quarto [@Allaire_Quarto_2024] para su publicación.

## Análisis

```{r}
#| include: false
library(tidyverse)
```

```{r}
#| include: false
my_data <- read_rds(here::here("data/datos_indec.rds"))
```

```{r}
#| echo: false
# Filtración de región Gran Buenos Aires
my_data$REGION |> 
  unique()
temp_df <- my_data |>
  filter(REGION == 01) 
```

```{r}
#| include: false
nrow(temp_df)
```


```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  mean()
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  median()
```

```{r}
#| include: false
temp_df$ITF |> 
  range()
```

```{r}
#| echo: false
options(scipen = 999)
temp_df |> 
  ggplot(aes(ITF))+
  geom_histogram()+
  scale_x_log10()
```

En el gráfico anterior podemos observar que en la región de Gran Buenos Aires más de la mitad de los hogares se encuentra por encima de la media del ITF (Ingreso Total Familiar). Esto indica que la distribución de la riqueza en esta región es dispar, con una considerable proporción de hogares obteniendo ingresos superiores al promedio regional.
Respecto a la media del Ingreso Total Familiar en los hogares de esta región, este número es de $224,668.3. Este valor medio proporciona una perspectiva clara sobre el nivel económico promedio en Gran Buenos Aires, destacando la disparidad en la distribución de los ingresos. Aunque esta cifra es más alta que en algunas otras regiones del país, la existencia de una mayoría de hogares por encima de la media sugiere una economía regional bastante robusta.

```{r}
#| include: false
# Filtración de región Noroeste
my_data$REGION |> 
  unique()
temp_df <- my_data |>
  filter(REGION == 40) 
```

```{r}
#| include: false
nrow(temp_df)
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  mean()
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  median()
```
```{r}
#| include: false
temp_df$ITF |> 
  range()
```

```{r}
#| echo: false
options(scipen = 999)
temp_df |> 
  ggplot(aes(ITF))+
  geom_histogram()+
  scale_x_log10()
```

En la región Noroeste la distribución de la riqueza se encuentra dispar ya que la mayoria de los hogares se encuentra por encima de la media. 
Respecto a la media del Ingreso Total Familiar en los hogares de esta región, este número es de $269987 

```{r}
#| include: false
# Filtración de región Noreste
my_data$REGION |> 
  unique()
temp_df <- my_data |>
  filter(REGION == 41) 
```

```{r}
#| include: false
nrow(temp_df)
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  mean()
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  median()
```
```{r}
#| include: false
temp_df$ITF |> 
  range()
```

```{r}
#| echo: false
options(scipen = 999)
temp_df |> 
  ggplot(aes(ITF))+
  geom_histogram()+
  scale_x_log10()
```

En este gráfico de la región Noreste se puede observar que hay picos en la cantidad de hogares con ITF (Ingreso Total Familiar) mayores a la media, pero la brecha no es tan amplia.
Respecto a la media del Ingreso Total Familiar en los hogares de esta región, este número es de $209,483.7. Esta cifra, aunque es una de las más bajas en comparación con otras regiones del país, proporciona una visión clara de la situación económica en el Noreste. A pesar de que los ingresos son menores, la presencia de picos en el gráfico muestra que hay segmentos de la población que logran superar el promedio.

```{r}
#| include: false
# Filtración de región Cuyo
my_data$REGION |> 
  unique()
temp_df <- my_data |>
  filter(REGION == 42) 
```

```{r}
#| include: false
nrow(temp_df)
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  mean()
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  median()
```
```{r}
#| include: false
temp_df$ITF |> 
  range()
```

```{r}
#| echo: false
options(scipen = 999)
temp_df |> 
  ggplot(aes(ITF))+
  geom_histogram()+
  scale_x_log10()
```

En este gráfico de la región de Cuyo se puede observar que, aunque hay picos en la cantidad de hogares con ITF (Ingreso Total Familiar) mayores a la media, el gráfico se encuentra relativamente nivelado.
Respecto a la media del Ingreso Total Familiar en los hogares de esta región, este número es de $252,978.3. Esta cifra indica un nivel de ingreso promedio que, si bien no es el más alto del país, muestra una posición intermedia que podría reflejar una economía regional relativamente estable. Además, la nivelación del gráfico sugiere una menor desigualdad en comparación con otras regiones donde los ingresos pueden estar más polarizados. 

```{r}
#| include: false
# Filtración de región Pampeana
my_data$REGION |> 
  unique()
temp_df <- my_data |>
  filter(REGION == 43) 
```

```{r}
#| include: false
nrow(temp_df)
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  mean()
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  median()
```
```{r}
#| include: false
temp_df$ITF |> 
  range()
```

```{r}
#| echo: false
options(scipen = 999)
temp_df |> 
  ggplot(aes(ITF))+
  geom_histogram()+
  scale_x_log10()
```

En este gráfico de la región Pampeana, se puede observar que la mayor cantidad de hogares se concentra por encima de la media del ITF (Ingreso Total Familiar).
Respecto a la media del Ingreso Total Familiar en los hogares de esta región, este número es de $211,733.7. 

```{r}
#| include: false
# Filtración de región Patagonica
my_data$REGION |> 
  unique()
temp_df <- my_data |>
  filter(REGION == 44) 
```

```{r}
#| include: false
nrow(temp_df)
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  mean()
```

```{r}
#| include: false
temp_df$ITF |> # Ingreso Total Familiar
  median()
```

```{r}
#| include: false
temp_df$ITF |> 
  range()
```

```{r}
#| echo: false
options(scipen = 999)
temp_df |> 
 ggplot(aes(ITF))+
  geom_histogram()+
  scale_x_log10()

```

En este gráfico de la región Patagónica, se puede observar que la mayor cantidad de hogares se encuentra por encima de la media del ITF (Ingreso Total Familiar). Esta tendencia indica que una gran proporción de las familias en esta región disfruta de ingresos superiores al promedio, lo cual refleja una situación económica relativamente favorable en comparación con otras áreas del país.
En cuanto a la media del Ingreso Total Familiar en los hogares de esta región, este valor asciende a $343,434.3. Este dato es significativo porque no solo señala una media elevada, sino que también resalta la disparidad económica entre la región Patagónica y otras regiones con menores niveles de ingreso familiar. 
             
```{r}
#| include: false
temp_df <- my_data |> 
  mutate(REGION2 = 
           case_when(
             REGION == 1 ~ "GBA",
             REGION == 40 ~ "Noroeste",
             REGION == 41 ~ "Noreste",
             REGION == 42 ~ "Cuyo",
             REGION == 43 ~ "Pampeana",
             REGION == 44 ~ "Patagonia"))


```

```{r}
#| include: false
temp_df |>
  group_by(REGION2) |>
  summarise(
    media = mean(ITF),
    mediana = median(ITF)
  )
```

```{r}
#| echo: false
temp_df %>%  
  group_by(REGION2) %>%  
  summarize(
    ITF = mean(ITF)
  ) |> 
  ggplot(aes(x=REGION2,y=ITF, fill = REGION2))+
    geom_col()
```
             
En este último gráfico, podemos observar el Ingreso Total Familiar desglosado por todas las regiones. Este gráfico proporciona una visión clara y detallada de las variaciones en los ingresos familiares entre las diferentes áreas geográficas,  facilitando la comparación directa entre las distintas zonas, destacando tanto las similitudes como las disparidades en los niveles de ingreso familiar a lo largo del territorio analizado. 
             
## Conclusiones
Pudimos observar que sí existe una amplia diferencia entre las diversas regiones del país, especialmente entre la región de la Patagonia y la región Noreste. La Patagonia se destaca por tener los índices de ITF (Ingreso Total Familiar) más altos, mientras que la región Noreste se encuentra en el extremo opuesto, ocupando el lugar más bajo en cuanto a este indicador.
Aun así, la región Pampeana se encuentra muy cercana al Noreste en los niveles más bajos.
Estas diferencias en el ITF tienen profundas implicaciones para la calidad de vida de los habitantes de cada región. En la Patagonia, los altos ingresos permiten un mayor acceso a bienes y servicios, mejor educación y atención sanitaria, y una mayor estabilidad económica. Por otro lado, en la región Noreste, los ingresos más bajos pueden limitar el acceso a estos mismos beneficios, afectando el bienestar general de las familias.

Por otro lado, pudimos encontrar que las medias varían en más de 100,000$ entre las regiones. Las regiones del Noreste (209,483.7) y la Pampeana (211,733.7) son las que tienen los valores más bajos, seguidas por el AMBA (224,668.3), Cuyo (252,978.3) y Noroeste (269,987). Amplia y claramente destacada se ubica la región Patagónica (343,434.3).